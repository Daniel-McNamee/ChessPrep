<UserControl x:Class="ChessProject.Views.GameBrowserView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="#1E1E1E">

    <Border Padding="16">

        <Grid>

            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Title -->
            <TextBlock Text="Chess.com Game Browser"
                       FontSize="20"
                       FontWeight="Bold"
                       Foreground="White"
                       Margin="0,0,0,12"/>

            <!-- Search -->
            <StackPanel Grid.Row="1"
                        Orientation="Horizontal"
                        Margin="0,0,0,12">

                <TextBox Width="200"
                         Margin="0,0,12,0"
                         Text="{Binding Username}"
                         ToolTip="Enter Chess.com username"/>

                <Button Content="Search"
                        Width="100"
                        Command="{Binding SearchPlayerCommand}"/>

            </StackPanel>

            <!-- Archives + Games -->
            <Grid Grid.Row="2">

                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="2*"/>
                </Grid.ColumnDefinitions>

                <!-- Archives -->
                <StackPanel Grid.Column="0">

                    <TextBlock Text="Game Archives"
                               FontWeight="Bold"
                               Foreground="White"
                               Margin="0,0,0,8"/>

                    <ListBox ItemsSource="{Binding Archives}"
                             SelectedItem="{Binding SelectedArchive}"
                             DisplayMemberPath="DisplayName"
                             Height="400"/>

                    <Button Content="Load Archive"
                            Margin="0,8,0,0"
                            Command="{Binding LoadArchiveCommand}"/>

                </StackPanel>

                <!-- Games -->
                <StackPanel Grid.Column="1"
                            Margin="16,0,0,0">

                    <TextBlock Text="Games"
                               FontWeight="Bold"
                               Foreground="White"
                               Margin="0,0,0,8"/>

                    <ListBox ItemsSource="{Binding Games}"
                             SelectedItem="{Binding SelectedGame}"
                             Height="400">

                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding}"
                                           Padding="4"/>
                            </DataTemplate>
                        </ListBox.ItemTemplate>

                    </ListBox>

                </StackPanel>

            </Grid>

        </Grid>

    </Border>
</UserControl>