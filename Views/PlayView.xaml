<UserControl x:Class="ChessProject.Views.PlayView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:conv="clr-namespace:ChessProject.Converters">

    <UserControl.Resources>

        <conv:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter"/>

        <!-- INPUT STYLE -->
        <Style x:Key="InputBoxStyle" TargetType="TextBox">
            <Setter Property="Height" Value="35"/>
            <Setter Property="Background" Value="#1E1E1E"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#555"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="8"/>
            <Setter Property="Margin" Value="0,5,0,0"/>
        </Style>

        <!-- TIME PILL -->
        <Style x:Key="TimePillStyle" TargetType="ToggleButton">

            <Setter Property="Margin" Value="8"/>
            <Setter Property="Padding" Value="18,10"/>
            <Setter Property="MinWidth" Value="100"/>
            <Setter Property="MinHeight" Value="40"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>

            <Setter Property="Background" Value="#3A3A3A"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#555"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Cursor" Value="Hand"/>

            <Setter Property="Template">
                <Setter.Value>
                    <!-- Using ToggleButton to manage selected state -->
                    <ControlTemplate TargetType="ToggleButton">

                        <Border x:Name="Bd"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="12">

                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>

                        </Border>

                        <ControlTemplate.Triggers>

                            <!-- HOVER -->
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#505050"/>
                                <Setter TargetName="Bd" Property="BorderBrush" Value="#777"/>
                            </Trigger>

                            <!-- PRESSED -->
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#2A2A2A"/>
                            </Trigger>

                            <!-- SELECTED -->
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#2F4F2F"/>
                                <Setter TargetName="Bd" Property="BorderBrush" Value="#7CFC00"/>
                                <Setter TargetName="Bd" Property="BorderThickness" Value="2"/>
                            </Trigger>

                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>

        </Style>

        <!-- PLAY BUTTON -->
        <Style x:Key="PrimaryButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#4CAF50"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="BorderThickness" Value="0"/>
        </Style>

    </UserControl.Resources>

    <Grid Background="#2A2A2A">

        <!-- MAIN MODE MENU -->
        <StackPanel Visibility="{Binding IsSetupVisible, Converter={StaticResource BoolToVisibilityConverter}, ConverterParameter=Invert}"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    Width="300">

            <TextBlock Text="Play"
                       FontSize="24"
                       FontWeight="Bold"
                       Foreground="White"
                       HorizontalAlignment="Center"
                       Margin="0,0,0,20"/>

            <Button Content="♟ Local 2 Player"
                    Height="50"
                    Command="{Binding StartLocalModeCommand}"/>

            <Button Content="📂 Saved Positions"
                    Height="50"
                    IsEnabled="False"/>

            <Button Content="🧩 Puzzles"
                    Height="50"
                    IsEnabled="False"/>
        </StackPanel>

        <!-- SETUP MENU -->
        <Border Width="500"
                Padding="30"
                Background="#2A2A2A"
                CornerRadius="12"
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                Visibility="{Binding IsSetupVisible, Converter={StaticResource BoolToVisibilityConverter}}">

            <StackPanel HorizontalAlignment="Center">

                <TextBlock Text="Game Setup"
                           FontSize="26"
                           FontWeight="Bold"
                           Foreground="White"
                           HorizontalAlignment="Center"
                           Margin="0,0,0,20"/>

                <!-- PLAYERS NAMES -->
                <TextBlock Text="White Player" Foreground="#CCCCCC"/>
                <TextBox Text="{Binding WhitePlayer}" Style="{StaticResource InputBoxStyle}"/>

                <TextBlock Text="Black Player" Foreground="#CCCCCC" Margin="0,10,0,0"/>
                <TextBox Text="{Binding BlackPlayer}" Style="{StaticResource InputBoxStyle}"/>

                <!-- TIME CONTROLS -->
                <TextBlock Text="Time Controls"
                           FontSize="18"
                           FontWeight="Bold"
                           Foreground="White"
                           HorizontalAlignment="Center"
                           Margin="0,20,0,10"/>

                <!-- BULLET -->
                <TextBlock Text="🚀 Bullet" Foreground="#BBBBBB" Margin="0,10,0,5"/>
                <ItemsControl ItemsSource="{Binding BulletTimes}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <ToggleButton Content="{Binding Display}"
                                          Style="{StaticResource TimePillStyle}"
                                          IsChecked="{Binding IsSelected}"
                                          Command="{Binding DataContext.SelectTimeControlCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                          CommandParameter="{Binding}"/>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>

                <!-- BLITZ -->
                <TextBlock Text="⚡ Blitz" Foreground="#BBBBBB" Margin="0,15,0,5"/>
                <ItemsControl ItemsSource="{Binding BlitzTimes}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <ToggleButton Content="{Binding Display}"
                                          Style="{StaticResource TimePillStyle}"
                                          IsChecked="{Binding IsSelected}"
                                          Command="{Binding DataContext.SelectTimeControlCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                          CommandParameter="{Binding}"/>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>

                <!-- RAPID -->
                <TextBlock Text="⏱ Rapid" Foreground="#BBBBBB" Margin="0,15,0,5"/>
                <ItemsControl ItemsSource="{Binding RapidTimes}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <ToggleButton Content="{Binding Display}"
                                          Style="{StaticResource TimePillStyle}"
                                          IsChecked="{Binding IsSelected}"
                                          Command="{Binding DataContext.SelectTimeControlCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                          CommandParameter="{Binding}"/>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>

                <!-- START -->
                <Button Content="Start Game"
                        Command="{Binding StartLocalGameCommand}"
                        Height="45"
                        Margin="0,25,0,0"
                        Style="{StaticResource PrimaryButtonStyle}"/>

            </StackPanel>
        </Border>

    </Grid>
</UserControl>
